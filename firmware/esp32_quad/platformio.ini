[env:arduino_nano_esp32]
platform = espressif32
board = arduino_nano_esp32
framework = arduino

monitor_speed = 115200

lib_deps =
    ; espressif/esp32-arduino-lib-builder ; add ESP-NOW deps here later
